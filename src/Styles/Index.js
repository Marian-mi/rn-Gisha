import { StyleSheet } from "react-native";

export const colors = {
    Primary: '#de1f26'
}

export const TextStyles = StyleSheet.create({
    Title1: {
        color: 'white',
        fontWeight: "700",
        fontSize: 16,
    }
})